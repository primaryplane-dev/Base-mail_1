

---

# stMenu.bas 詳細解析

## 1. 概要

stMenu.basは、Excel VBAで実装されたメインメニュー的なモジュールであり、主にデータ生成・CSV出力・メール送信などの一連の業務処理を自動化する役割を担っています。  
主なサブルーチンは以下の通りです。

- `cmdEnd_Click`: 終了処理
- `cmdNew_Click`: 新規データ生成・送信処理
- `cmdUpdate_Click`: 更新処理

---

## 2. データ生成部分の詳細

### 2.1 工場送付用ファイルの存在チェック

```vb
If Dir(ThisWorkbook.Path & "\工場送付用(JAN抽出)*.xlsx") = "" Then
    MsgBox ("工場送付用(JAN抽出)が見つかりません")
    Exit Sub
End If
```
- 必要なExcelファイルが存在しない場合は警告を出して処理を中断します。

### 2.2 データベース接続とデータ取得

```vb
CN.CursorLocation = adUseClient
CN.Open P_ConnectString

ReDim rec(0)
ReDim arrChangeKJ(0)
If Not fncGetUpdData(CN, arrChangeKJ, rec) Then GoTo Exit_Update
If UBound(arrChangeKJ) = 0 Or UBound(rec) = 0 Then GoTo Exit_Update
```
- ADODBを用いてDB接続し、`fncGetUpdData`で更新対象の工場コード配列（`arrChangeKJ`）とレコード配列（`rec`）を取得します。

### 2.3 排他チェックと排他ファイル作成

```vb
For i = 1 To UBound(arrChangeKJ)
    lWk = fncEditChk(arrChangeKJ(i))
    ' ...省略...
Next
If lWk2 > 0 Then
    ' 排他ファイル強制削除の選択肢
End If

For i = 1 To UBound(arrChangeKJ)
    Call subMakeEditFile(arrChangeKJ(i))
Next
```
- 各工場ごとに排他ファイルの存在をチェックし、必要に応じて強制削除・新規作成を行います。

### 2.4 データ更新・本文生成

```vb
strBody = ""
For i = 1 To UBound(rec)
    If Not strWk = "" Then strWk = strWk & "@@@"
    strWk = strWk & fncMakebody(rec(i).KCD, rec(i).HINM)
    Call subUpdate(CN, rec(i))
Next
```
- 各レコードごとに本文（`fncMakebody`で生成）を`strWk`に`@@@`区切りで連結し、同時にDB更新（`subUpdate`）を行います。

### 2.5 CSVファイル生成・送信

```vb
Dim FSO As New Scripting.FileSystemObject
For i = 1 To UBound(arrChangeKJ)
    Call subWriteCSV(arrChangeKJ(i), FSO)
    Call subSendCSV(arrChangeKJ(i), FSO)
Next
Set FSO = Nothing
```
- 各工場ごとにCSVファイルを生成し、ファイルサーバや工場の共有フォルダへ送信します。

---

## 3. メール送信処理（複数工場の場合）

### 3.1 本文の分割と工場ごとの抽出

```vb
sSp = Split(strWk, "@@@")
For i = 1 To UBound(arrChangeKJ)
    strBody = ""
    For j = 0 To UBound(sSp)
        sSp2 = Split(sSp(j), "@")
        If sSp2(0) = arrChangeKJ(i) Then
            If Not strBody = "" Then strBody = strBody & vbLf
            strBody = strBody & sSp2(1)
        End If
    Next
    ' ...省略...
Next
```
- `strWk`には工場ごとの本文が`@@@`で連結されており、`Split`で分割して`sSp`配列に格納します。
- 各工場（`arrChangeKJ(i)`）ごとに、該当する本文だけを`sSp2(1)`として`strBody`にまとめます。
- 複数工場に追加があった場合、それぞれの工場ごとに本文を抽出し、**工場ごとに個別のメール本文**を作成します。

### 3.2 メールアドレス取得と送信

```vb
If Not strBody = "" Then
    fromAddress = "": toAddress = ""
    Call subGetMailAdd(arrChangeKJ(i), fromAddress, toAddress)
    Call subSendMail(fromAddress, toAddress, "", "IJPデータ追加通知", strBody)
End If
```
- 本文が存在する工場について、`subGetMailAdd`で送信元・送信先アドレスを取得し、`subSendMail`でメール送信します。
- **工場ごとに1通ずつ**メールが送信されます。

---

## 4. ポイントまとめ

- データ生成・CSV出力・メール送信までを一括で自動化。
- 複数工場に追加があった場合も、**工場ごとに本文を作成し、個別にメール送信**する設計。
- 本文の生成・分割・抽出ロジックにより、各工場に適切な内容が送信されるようになっています。

---



# subSendMail の詳細

## 概要

`subSendMail` は、VBAからSMTPサーバーを利用してメールを送信するためのサブルーチンです。  
CDO（Collaboration Data Objects）を利用し、指定した送信元・宛先・件名・本文でメールを送信します。

---

## 引数

- **fromAddress**: 送信元メールアドレス
- **toAddress**: 宛先メールアドレス
- **ccAddress**: CCメールアドレス（空文字可）
- **subject**: メール件名
- **Body**: メール本文（テキスト形式）

---

## 主な処理内容

1. **CDO.Configuration オブジェクトの生成・設定**
    - `CreateObject("CDO.Configuration")` で構成オブジェクトを生成
    - SMTPサーバー（例: `10.2.1.12`）、ポート（25）、認証方式（Basic）、SSL未使用などを設定
    - 送信ユーザー名として `fromAddress` を利用
    - 必要に応じてパスワード設定も可能（コメントアウト済み）

2. **CDO.Message オブジェクトの生成・設定**
    - `CreateObject("CDO.Message")` でメッセージオブジェクトを生成
    - 構成情報をセットし、送信元・宛先・CC・件名・本文を設定
    - 本文の文字コードは `shift-jis`（日本語対応）

3. **メール送信**
    - `.Send` メソッドでメール送信
    - エラー発生時はデバッグ出力（`Debug.Print`）

4. **オブジェクトの解放**
    - 使用したオブジェクトを `Nothing` で解放

---

## ポイント

- **SMTPサーバー**は社内専用（例: `10.2.1.12`）を利用
- **パスワード認証**は必要に応じて設定可能（デフォルトはコメントアウト）
- **本文はテキスト形式**（`TextBody`）で送信、文字コードは `shift-jis`
- **CCは任意**（空文字の場合は設定されない）
- **エラー処理**は簡易的（エラー時はデバッグ出力のみ）

---

## 複数工場への送信との関係

- 呼び出し元で工場ごとに本文・宛先を生成し、**工場ごとに1通ずつ**この `subSendMail` が呼ばれる
- 複数工場に追加があった場合も、**各工場ごとに個別のメールが送信**される

---

# subSendCSV の詳細

## 概要

`subSendCSV` は、生成したCSVファイルを工場のファイルサーバーや各PCに配布・転送するためのサブルーチンです。  
VBAの `Scripting.FileSystemObject` を利用し、ネットワーク経由で複数の宛先にファイルをコピーします。

---

## 引数

- **i_BUTUKCD**: 工場コード（配布先を特定するために使用）
- **FSO**: `Scripting.FileSystemObject` の参照（ファイル操作用）

---

## 主な処理内容

1. **配布先情報の取得**
    - `fncGetSVPath(i_BUTUKCD)` で工場ファイルサーバーパスを取得
    - `fncGetIPAdd(i_BUTUKCD)` でIJP（配布先PC）のIPアドレスリストを取得（カンマ区切り）

2. **認証情報の設定**
    - ID/パスワード（例: "kgk"/"kgk"）を設定
    - テスト用の管理者アカウントもコメントで用意

3. **配布先ごとの処理**
    - IPアドレスリストを分割し、各PCごとにループ
    - 配布先がローカル（C:\）の場合とネットワークPCの場合で処理を分岐

4. **ローカル（C:\）の場合**
    - 保存先パスがなければ `MkDir` で作成
    - 既存CSVがあればバックアップ（BKフォルダに日付付きで退避）
    - サーバー側のCSVをコピー

5. **ネットワークPCの場合**
    - `net use` コマンドでネットワークドライブに接続（ID/パスワード指定）
    - 必要に応じて保存先パスを作成
    - 既存CSVがあればバックアップ
    - サーバー側のCSVをコピー
    - コピー後、`net use ... /delete` で接続解除

6. **全配布先への転送が完了したら終了**

---

## ポイント

- **複数PC/サーバーへの一括配布**に対応（IPアドレスリストで複数指定可）
- **既存ファイルのバックアップ**（BKフォルダに日付付きで退避）
- **ネットワーク経由のファイルコピー**（`net use`で認証付き接続）
- **テスト用・本番用の分岐**がコメントで用意されている
- **ファイル/フォルダの存在チェック**や作成処理も自動化

---

## 関連

- 配布先パスやIPアドレスの取得には `fncGetSVPath`・`fncGetIPAdd` を利用
- ファイル名や保存先パスはグローバル変数 `P_SavePath`・`P_CsvName` などで管理

---

# Bas_List.bas の subMain 詳細解析

## 概要

`Bas_List.bas` の `subMain` サブルーチンは、リスト編集処理のメインエントリーポイントです。  
このサブルーチンは、リスト編集の前処理・本処理・後処理を順に実行します。

---

## 処理の流れ

### 1. 前処理（subBeforeEdit）

- `subBeforeEdit` を呼び出します。
- ※この内容は本ファイルには記載がありませんが、リスト編集前の初期化やクリア処理などが想定されます。

---

### 2. 本処理（subEditList）

#### 2.1 シートの初期化

- `subInitialize` を呼び出し、雛型シート（`STHina`）の内容を編集用シート（`stList`、`stWork`）にコピーします。
- 書式（文字列型、中央揃え、フォントサイズなど）を再設定します。

#### 2.2 データベース接続

- ADODBを使い、`P_ConnectString` で指定されたDBに接続します。

#### 2.3 データ取得

- SQL文を組み立て、`LIBWMF.WGIP01` テーブルから
  - 削除フラグ（`GIDELT <> 'X'`）
  - 区分（`GIKBN = '2'`）
  - プロジェクト区分（`GIPROJ <> '9'`）
  で絞り込み、工場コード・品番順でデータを取得します。

#### 2.4 データのシート展開

- 取得したレコードを1件ずつ、`stList` および `stWork` シートの3行目以降に転記します。
  - 1列目: 品番（`GIHNO`）
  - 2列目: 工場名（`fncGetKJNM(GIKJCD)` でコードから名称変換）
  - 3列目: 品名（`GIHINM`）
  - 4列目: ライン名（`GILINM`）

- 罫線（`Borders.LineStyle = xlContinuous`）を引きます。

#### 2.5 DB切断・画面整形

- レコードセット・コネクションをクローズし、オブジェクトを解放します。
- シートの先頭セルを選択し、画面のスクロール位置をリセットします。

---

### 3. 後処理（subAfterEdit）

- `subAfterEdit` を呼び出します。
- ※この内容も本ファイルには記載がありませんが、編集後の整形や追加処理が想定されます。

---

## ポイント

- **雛型シートから編集用シートへのコピー**と**書式再設定**で、Excelの書式崩れを防止。
- **DBから最新のリストデータを取得し、シートに展開**することで、常に最新状態を反映。
- **工場コード→工場名変換**は `fncGetKJNM` を利用し、ユーザーに分かりやすい表示を実現。
- **罫線やフォントなどの書式も自動設定**されるため、見た目も整ったリストが生成される。

---

## まとめ

`subMain` は、リスト編集の一連の流れ（初期化→DB取得→シート展開→整形）を自動化する中心的なサブルーチンです。  
`stMenu` の `cmdUpdate` から呼ばれることで、ユーザーがボタン一つでリストの最新化・整形を実行できる仕組みとなっています。

---
# stList.bas 詳細解析

## 概要

`stList.bas` は、Excelシート上でリストデータの編集・更新・削除・ラベル出力などを行うためのVBAモジュールです。  
主に「リスト編集画面」のユーザー操作（ボタンやセル選択）に対応したサブルーチンが実装されています。

---

## 主なサブルーチンと機能

### 1. cmdBack_Click

- メニュー画面（`stMenu`）に戻るボタンの処理。
- `stMenu.Select` で画面遷移。

---

### 2. cmdLayout9_Click

- レイアウト9画面への遷移ボタン。
- クリップボードのコピー状態を解除し、`subMainLay9` を実行後、`stLay9` シートへ遷移。

---

### 3. cmdUpdate_Click

#### 3.1 入力・変更チェック

- `fncChk(arrChangeKJ)` で入力内容と変更内容をチェック。
  - 入力値が数値か、存在するコードかを検証。
  - ワークシート（stWork）と比較し、削除・変更があった工場コードを `arrChangeKJ` に格納。
  - 変更・削除がなければ処理終了。

#### 3.2 排他ファイルの存在確認・強制削除

- 各工場ごとに排他ファイルの存在を `fncEditChk` で確認。
- 排他中の場合、強制削除の選択肢をユーザーに提示し、必要に応じて `subDeleteEditFile` で削除。

#### 3.3 排他ファイルの作成

- 各工場ごとに `subMakeEditFile` で排他ファイルを作成。

#### 3.4 AS（基幹システム）へのデータ更新

- `subSetData(delRec)` で削除レコード情報を取得・更新。

#### 3.5 CSVファイル生成・配布・排他ファイル削除

- 各工場ごとに
  - `subWriteCSV` でCSVファイルを作成
  - `subSendCSV` でファイルサーバや工場PCへ配布
  - `subDeleteEditFile` で排他ファイルを削除

#### 3.6 削除があった工場へのメール送信

- 各工場ごとに、削除された商品情報を本文にまとめ、`subGetMailAdd` でアドレス取得、`subSendMail` で「IJPデータ削除通知」メールを送信。

#### 3.7 データ再表示

- `subMain` を再度呼び出し、リストを最新状態に更新。

---

### 4. cmdLabel_Click

- ラベル出力用のサブルーチン。
- データが存在しない場合は警告、存在すれば `subMainLabel` を実行。

---

### 5. fncChk（入力・変更チェック）

- 入力値の妥当性（数値・存在チェック）をDB照会で検証。
- ワークシートと編集シートの差分を比較し、削除・変更があった工場コードを抽出。
- 変更・削除がなければ `False` を返す。

---

### 6. Worksheet_SelectionChange

- セル選択時のイベントハンドラ。
- 4列目（ライン名）選択時はライン選択フォーム（`frmSelectLINM`）を表示し、選択結果を反映。
- 5列目（ラベル出力フラグ）選択時は「○」のON/OFF切替。

---

### 7. cmdLabelAll_Click

- 5列目（ラベル出力フラグ）の一括ON/OFF切替。

---

### 8. subOpen

- ライン名選択フォーム（`frmSelectLINM`）の表示。

---

## ポイント

- **排他制御**：排他ファイルの作成・削除で多重編集を防止。
- **DB連携**：入力値や商品コードの存在チェック、データ更新はDBと連携。
- **CSV配布・メール通知**：工場ごとにCSV配布・削除通知メールを自動化。
- **ユーザー操作性**：ボタンやセル選択で直感的に操作できるUI設計。
- **ラベル出力**：5列目の「○」でラベル出力対象を指定可能。

---

## まとめ

`stList.bas` は、リスト編集画面のユーザー操作に応じて、  
データの検証・排他制御・CSV配布・メール通知・ラベル出力など一連の業務処理を自動化するVBAモジュールです。  
業務フローの正確性とユーザー利便性を両立した設計となっています。


# 「削除があった工場にメール送信」処理の詳細

## 概要

`stList.bas` の `cmdUpdate_Click` 内にある「削除があった工場にメール送信」部分は、  
リスト編集で商品データの削除が発生した場合に、該当工場ごとに削除通知メールを自動送信する処理です。

---

## 処理の流れ

1. **工場ごとに本文生成**
    - ループで `arrChangeKJ`（削除・変更があった工場コードリスト）を走査。
    - 各工場ごとに本文（`strBody`）を初期化。
    - `delRec` 配列（削除された商品情報の配列）を走査し、該当工場（`delRec(j).KCD = arrChangeKJ(i)`）の削除商品名（`delRec(j).HINM`）を `fncMakebody2` で本文化し、`strBody` に連結。
    - 複数商品が削除された場合は改行（`vbLf`）で区切ってまとめる。

2. **メールアドレスの取得**
    - 本文が空でなければ、`subGetMailAdd` で該当工場の送信元・宛先メールアドレスを取得。

3. **メール送信**
    - `subSendMail` を呼び出し、件名「IJPデータ削除通知」、本文（削除商品リスト）でメールを送信。

---

## ポイント

- **工場ごとに1通ずつ**メールが送信される（複数工場で削除があれば、その数だけメール送信）。
- 本文には**その工場で削除された商品名がすべて列挙**される。
- 本文生成には `fncMakebody2` を利用し、商品名などを整形。
- メールアドレスは `subGetMailAdd` で工場ごとに自動取得。
- 実際の送信は `subSendMail`（CDO経由のSMTPメール送信）で行う。

---

## サンプルイメージ

- 件名: `IJPデータ削除通知`
- 本文例: