# システム概要

言語：VBA
拠点ごとのデータ処理とメール通知を自動化するシステム

---

## 主な機能

### 1. データ処理
- 各種 Bas_*.bas モジュールで、データの追加・削除・更新などの業務処理を実施。
- stList などのシートモジュールで、ユーザー操作やデータチェック、排他制御、CSV出力などを管理。

### 2. メールアドレス取得
- `subGetMailAdd` サブルーチンで、拠点コード（KCD）に応じた送信元・宛先アドレスを、各拠点の「メーリングリスト.xlsx」から取得。
  - 送信元（fromAddress）は、リスト内の「BASE」行のアドレス（最初に見つかったもの）。
  - 宛先（toAddress）は、2列目に記載された全アドレス（カンマ区切り）。

### 3. メール本文生成
- `fncMakebody` / `fncMakebody2`（Bas_Mail.bas） で、処理内容（追加・削除など）に応じた定型文の本文を作成。

### 4. メール送信
- `subSendMail` （Bas_Mail.bas）で、上記で取得した送信元・宛先・本文などをもとに、CDOを利用してSMTPサーバー経由でメールを送信。

### 5. 拠点選択・ユーザーインターフェース
- `frmSelectKJNM ` などのユーザーフォームで拠点選択。
- シート（stList等）での操作により、工場ごとの処理やメール送信が自動化。** ←ここを改修 **

### 6. 排他制御・後処理
- `subMakeEditFile`/ `subDeleteEditFile `で、排他ファイルを管理。
- ThisWorkbook の Workbook_BeforeClose で全拠点の排他ファイルを削除し、Excel終了処理を制御。

---

## メールアドレス取得の詳細

- `subGetMailAdd` は、KCD（拠点コード）をもとにサーバーパスを決定し、該当する「メーリングリスト.xlsx」を開きます。
- 2行目以降を走査し、2列目が空でなければ宛先リストに追加。
- 1列目が「BASE」かつ送信元が未設定の場合、そのアドレスを送信元にセットします。
- 最後にExcelファイルを閉じ、オブジェクトを解放します。

---

## システム全体の流れ

1. ユーザーが拠点や商品を選択・編集. データ処理（追加・削除・更新など）
2. メールアドレス取得（`subGetMailAdd`）
3. メール本文生成（`fncMakebody` など）
4. メール送信（`subSendMail`）

1. ユーザーが拠点や商品を選択・編集
2. stList等で入力・変更チェック、排他制御
3. データ処理・CSV出力・サーバー転送
4. 削除・変更があった拠点ごとにメール送信
- ・subGetMailAddでアドレス取得
- ・fncMakebody2で本文生成
- ・subSendMailで送信
- ・Excel終了時に排他ファイルを全拠点分削除（ThisWorkbook）

---

## 補足

- サーバーパスや拠点名の判定には、`Bas_Public.bas` の共通関数・定数が利用されています。
- 各拠点ごとに「メーリングリスト.xlsx」を持ち、柔軟な宛先管理が可能です。
- メール送信のトリガーや拠点選択は、ユーザーフォームやシート上の操作、または他のサブルーチンから行われます。

---